<?php
/**
 * Category Badge — docs/biblioteca_componentes.md §5.
 *
 * Responsabilidad: mostrar la categoría de un artículo, opcionalmente
 * como filtro clickeable. Variante `overlay` (sobre imagen) o `inline`
 * (cabecera de artículo).
 *
 * Pendiente de implementación (Sprint 7, docs/roadmap.md).
 *
 * @package SES_Abogados
 */

defined( 'ABSPATH' ) || exit;
