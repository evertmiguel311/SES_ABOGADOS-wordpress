<?php
/**
 * Timeline (opcional) — docs/biblioteca_componentes.md §3.
 *
 * Responsabilidad: mostrar hitos de trayectoria de la firma en orden
 * cronológico, SOLO si el cliente aprueba este contenido — decisión
 * todavía abierta (docs/componentes.md).
 *
 * Pendiente de aprobación de contenido antes de cualquier implementación
 * — no asignado a un sprint del roadmap mientras siga abierta.
 *
 * @package SES_Abogados
 */

defined( 'ABSPATH' ) || exit;
