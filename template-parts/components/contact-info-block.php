<?php
/**
 * Contact Info Block — docs/biblioteca_componentes.md §8.
 *
 * Responsabilidad: bloque reutilizable con teléfono, WhatsApp, correo y
 * dirección, leyendo de la fuente única de datos de contacto (Options
 * Page ACF). El Footer puede mostrar menos campos que la página de
 * Contacto (`show`), en layout `stacked` o `inline`.
 *
 * Pendiente de implementación (Sprint 8, docs/roadmap.md).
 *
 * @package SES_Abogados
 */

defined( 'ABSPATH' ) || exit;
